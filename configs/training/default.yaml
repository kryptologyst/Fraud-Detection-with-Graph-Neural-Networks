# @package training
_target_: src.train.trainer.Trainer

epochs: 100
batch_size: 1
learning_rate: 0.01
weight_decay: 5e-4
patience: 20
min_delta: 0.001
optimizer: adam
scheduler: cosine
warmup_epochs: 10
